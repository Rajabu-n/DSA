# Write your MySQL query statement below
select query_name,ROUND((SUM(rating/position))/Count(*),2) as quality ,ROUND(SUM(CASE WHEN rating <3 then 1 else 0 end)*100.0/COUNT(*),2)  as poor_query_percentage from Queries group by query_name;